{
  "name": "zenith-study",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir dist && cp -r components services App.tsx constants.ts index.html index.tsx metadata.json types.ts dist/"
  },
  "dependencies": {
    "@google/genai": "^1.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12"
  }
}